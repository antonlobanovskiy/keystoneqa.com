# keystoneqa.com

Marketing site for Keystone QA's mobile automation consultancy.

## Local preview

Open `index.html` in your browser or run a simple server:

```bash
python -m http.server 8000
```

Assets live in `assets/` (logo icon and wordmark). Update links or copy as needed.
